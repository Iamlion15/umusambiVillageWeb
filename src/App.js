import { BrowserRouter } from "react-router-dom";
import RoutingUtils from "./component/routing";

function App() {
  return (
    <BrowserRouter>
        <RoutingUtils/>
    </BrowserRouter>
  );
}

export default App;
